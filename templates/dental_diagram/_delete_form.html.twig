<form method="post" action="{{ path('app_dental_diagram_delete', {'id': dental_diagram.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dental_diagram.id) }}">
    <button class="btn">Delete</button>
</form>
